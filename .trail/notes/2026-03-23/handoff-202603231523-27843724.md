---
created: 2026-03-23T19:23:28.198Z
type: handoff
session: 27843724-5741-4324-b381-6de6c20dbef3
---

# Handoff 2026-03-23 15:23

> Session `27843724`

## Done

- **12-branch phase 3 stack merged** — PRs #100-#111 landed on main. Broker recovery, heartbeat enforcement, message provenance, revocation seal publishing, key rotation, biometric gating, deployment templates.
- **Docker verified** — daemon starts in container, health endpoint responds, env var config wiring fixed.
- **PR review feedback addressed** — 22 review comments resolved across all 12 PRs. Bugs fixed: timestamp comparison, liveness interval, session check ordering, auto-rotation wiring, revocation seal groupId lookup.
- **CLI parity analysis** — XMTP CLI (63 commands), Convos CLI (37 commands), signet CLI (34 commands) compared. Gaps identified and addressed in v1 architecture.
- **v1 architecture spec** — `.agents/plans/v1/v1-architecture.md`. Complete redesign:
  - Identity model: owner → admin → operator → credential → seal
  - Role hierarchy: operator / admin / superadmin
  - Access/encryption model with cryptographic key separation per operator
  - Privilege elevation via biometric gate (SE)
  - 30 permission scopes across 6 categories
  - Reusable policies (`xs policy create/list/info/update/rm`)
  - Seal protocol: chaining with inline diffs, message-seal binding, transparency
  - CLI redesign: `xs` with `chat`, `msg`, `cred`, `policy`, `wallet`, `key` groups
  - Action registry as transport-agnostic foundation
  - Resource IDs: `op_`, `inbox_`, `conv_`, `policy_`, `cred_`, `seal_`, `key_`, `msg_`, `xmtp_`
  - Global flags: `--json`, `--watch`, `--as`, `--op`, `--provider`, `--wallet`, `--policy`, `--allow`, `--deny`, `--force`, `--purge`, `--only`
  - Test strategy with security boundary tests
- **OWS integration plan** — `.agents/plans/v1/ows-integration.md`:
  - OWS as optional plugin, not dependency
  - Provider model: `internal` (signet vault, @noble crypto) vs `ows` (external vault, optional Rust NAPI)
  - Signet reads from OWS wallets without copying/modifying them
  - OWS-compatible file format (Keystore v3 extended) for internal wallets
  - Three-layer token separation: admin bootstrap → harness credential → OWS backing token
  - Wallet vs key distinction: wallets are source material, keys are signet-derived
  - Key derivation: BIP-39 mnemonic → BIP-44 EVM paths for XMTP + Ed25519 for seals
  - Export/import with encrypted backup (scrypt + AES-256-GCM)
  - Signing orchestration vision: one-time approval flows for cross-chain operations
  - Key material safety invariant: agents never see raw keys, NAPI boundary is security perimeter
  - XDG-compatible directory structure: `~/.xmtp/signet/`
- **Codex reviews** — architecture review (5 findings addressed) + token delivery design (three-layer separation, renewal, mutual auth)
- **Consistency check** — 9 inconsistencies between docs found and fixed
- **New GitHub issue #99** — Cloudflare Durable Objects adapter (deferred)
- **OWS reference repo** — cloned at `.reference/ows-core/`

## State

- Main branch clean with all 12 PRs merged
- On `feat/deployment-templates` branch locally (needs `gt sync`)
- Two v1 planning docs complete, reviewed, and consistent:
  - `.agents/plans/v1/v1-architecture.md`
  - `.agents/plans/v1/ows-integration.md`
- No uncommitted code changes

## Next

- [ ] Run `gt sync -f` to clean up merged branches locally
- [ ] Test Bun NAPI compatibility: `bun add @open-wallet-standard/core`
- [ ] Research how Convos handles MLS state management
- [ ] Research Convos iOS passkey flow for XMTP identity derivation
- [ ] Design `KeyBackend` interface — minimal abstraction for `internal` and `ows` providers
- [ ] Refactor `packages/keys/` to use OWS-compatible file format
- [ ] Implement CLI restructure — clean break to `xs` taxonomy
- [ ] Implement `operator` and `credential` models (replacing `session`)
- [ ] Implement `policy` as first-class resource
- [ ] Implement wallet provider system (`internal` + `ows` plugin)
- [ ] File issues for v1 architecture work
- [ ] Write threat model document
- [ ] Propose XIPs for seal content type and message-seal binding
